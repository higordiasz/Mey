const Discord = require('discord.js')
const config = require('../json/config.json')
const func = require("./functions")

module.exports = {
    name: "nickname",
    aliases: ["nk"],
    description: "Mudar o nickname.",
    async execute(msg, args, client) {

    }
}